FOR u IN 0..1 OUTBOUND CONCAT('User/', @username) GRAPH Follows
    FOR brillo IN 1 INBOUND u._id GRAPH Author
        SORT brillo.created_at DESC
        LIMIT @offset, @limit
        RETURN MERGE(brillo, { username: u._key })
